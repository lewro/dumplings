.page-top.clearfix
  .h-line
  %h1
    = header

  = link_to "/product_stock_locations", :class => "link-button float-right"  do
    %i.fa.fa-list
    = "#{t'stock.product_locations'}"

  = link_to "/stock_problems", :class => "link-button float-right"  do
    %i.fa.fa-list
    = "#{t'stock.problems'}"

  = link_to "/stock_supply_reductions", :class => "link-button float-right"  do
    %i.fa.fa-list
    = "#{t'stock.problems_2'}"

  = render :partial => "/generic/search", :locals => {:placeholder => "#{t'stock.search'}", :category => "stock"}
