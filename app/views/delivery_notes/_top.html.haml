.page-top.clearfix
  .h-line
  %h1
    = header

  = link_to "/delivery_notes/new", :class => "button" do
    %i.fa.fa-plus-circle
    = "#{t'delivery_note.add'}"

  = render :partial => "/generic/search", :locals => {:placeholder => "#{t'delivery_note.search'}", :category => "delivery_note" }
