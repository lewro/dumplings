.page-top.clearfix
  .h-line
  %h1
    = header

  = link_to "/client_orders/new", :class => "button"  do
    %i.fa.fa-plus-circle
    = "#{t'orders.new'}"

  = render :partial => "/generic/search", :locals => {:placeholder => "#{t'orders.search'}", :category => "client_order" }

