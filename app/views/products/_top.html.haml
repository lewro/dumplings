.page-top.clearfix
  .h-line
  %h1
    = header

  = link_to "/products/new", :class => "button" do
    %i.fa.fa-plus-circle
    = "#{t'product.add'}"

  = link_to "/products", :class => "link-button float-right" do
    %i.fa.fa-list
    = "#{t'products'}"

  = render :partial => "/generic/search", :locals => {:placeholder => "#{t'product.search'}", :category => "product" }

