= render :partial => 'generic/top', :locals => {:current_view => 'products'}
= render :partial => 'products/top', :locals => { :header => "#{t'product.title'} #{@product.id}" }
= render :partial => 'products/form', :locals => { :action =>  "#{t'actions.save'}", :remove => true }