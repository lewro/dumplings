.page-top.clearfix
  .h-line
  %h1
    = header

  = link_to "/tasks/new", :class => "button" do
    %i.fa.fa-plus-circle
    = "#{t'task.add'}"

  = link_to "/tasks", :class => "link-button float-right" do
    %i.fa.fa-list
    = "#{t'tasks'}"
