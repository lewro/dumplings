- printId = id_format(@offer.id, @client)

= render :partial => 'generic/top', :locals => {:current_view => 'offers'}
= render :partial => "offers/top", :locals => { :header => "#{t'offer.title'} #{printId}"}
= render :partial => "offers/form", :locals => { :action =>  "#{t'actions.save'}", :remove => true }
