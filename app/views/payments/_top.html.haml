.page-top.clearfix
  .h-line
  %h1
    = header

  = link_to "/payments/new", :class => "button" do
    %i.fa.fa-plus-circle
    = "#{t'payment.add'}"

  = link_to "/payments", :class => "link-button float-right" do
    %i.fa.fa-list
    = "#{t'payments'}"

  = render :partial => "/generic/search", :locals => {:placeholder => "#{t'payment.search'}", :category => "payment"}
